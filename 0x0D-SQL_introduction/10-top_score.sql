-- SQL script to list all records of the table second_table from the database hbtn_0c_0
-- Select all records from the table second_table, displaying both score and name, and order by score (top first)
SELECT score, name
FROM second_table
ORDER BY score DESC;
